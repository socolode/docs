---
sidebar_position: 4
sidebar_label: MicroPython API
title: "Beetle S1 - MicroPython API"
description: "Commonly used MicroPython standard libraries and APIs for Beetle S1 development"
keywords: ["Beetle S1", "MicroPython", "API", "Standard Library", "Python"]
---

import Link from '@docusaurus/Link';

# MicroPython API

The Beetle S1 runs on MicroPython and supports most MicroPython standard libraries. Below are commonly used modules for light effect development.

## Common Standard Libraries

### `machine` — Hardware Control

```python
from machine import Pin, PWM, ADC

# Set GPIO pin
pin = Pin(4, Pin.OUT)
pin.value(1)  # Output high

# PWM control
pwm = PWM(Pin(5))
pwm.freq(1000)  # Set frequency
pwm.duty(512)   # Set duty cycle
```

### `time` — Timing Control

```python
import time

time.sleep(1)        # Delay 1 second
time.sleep_ms(500)   # Delay 500 milliseconds
time.ticks_ms()      # Get current millisecond count
```

### `math` — Math Operations

```python
import math

# For generating smooth light animations
value = math.sin(angle)  # Sine wave
value = math.cos(angle)  # Cosine wave
```

## More Resources

The MicroPython official documentation provides a complete standard library reference:

- [MicroPython Documentation](https://docs.micropython.org/en/latest/)

For the Beetle S1 custom API, see the <Link to="./custom-api">Custom API</Link> section.